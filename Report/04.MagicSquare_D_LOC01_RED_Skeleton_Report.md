# MagicSquare_xx — D-LOC-01 RED 스켈레톤 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_xx |
| 문서 | `04.MagicSquare_D_LOC01_RED_Skeleton_Report.md` |
| 작성일 | 2026-06-04 |
| Phase | **RED** |
| Layer / Track | **entity** / **Logic** |
| Test ID | **D-LOC-01** |
| 선행 | `Report/03` (Cursor 설계), `README.md` (Dual-Track·구조) |
| Transcript | `Prompting/02.MagicSquare_Session4_Export_Transcript.md` |

---

## 1. 주제 (1문장)

**G1 부분 격자에서 빈칸(0) 두 칸의 1-index 좌표를 row-major로 반환하는 entity 로직을, RED 테스트 한 건부터 Test Loop에 올린다.**

---

## 2. Mom Test · PRD 정합 (고정)

| 구분 | 내용 |
|------|------|
| **진짜 문제** | 제출 전 10줄(↘↙ 포함) 검증 누락 → 채점 후 장시간 우연 탐색 |
| **이번 RED** | 빈칸 **위치 식별** (FR-LOC-01 권고) — Solver/Validator 본문 아님 |
| **G1** | 4×4, `0`×2, 1-index **(2,2)·(3,3)** (설계표·스켈레톤 Then 주석) |
| **Non-Goal** | `find_blank_coords` GREEN, boundary U-IN, D-VAL-01 일괄 |

---

## 3. C2C 추적 (Rule 1~3)

### Rule 1 — FR → Test ID

| FR (권고) | PRD 근거 | Test ID |
|-----------|----------|---------|
| FR-LOC-01 | §2 도메인 `0`×2, §2.1 G1 예시 | **D-LOC-01** |

> `docs/PRD.md`에 `FR-LOC-01` ID는 미등재. 추적용 권고 문안: *0인 칸 2개의 (행,열) 1-index, row-major 반환.*

### Rule 2 — To-Do (판단 1개)

| ID | 판단 |
|----|------|
| T-LOC-RED-1 | `find_blank_coords(grid_g1)` → `[(2,2),(3,3)]` (1-index, row-major)인가? |

### Rule 3 — Given / When / Then

| | 내용 |
|---|------|
| **Given** | `grid_g1` — G1, 0×2 |
| **When** | `find_blank_coords(grid_g1)` *(GREEN 대기)* |
| **Then** | `[(2,2),(3,3)]` |
| **RED Then (현재)** | `pytest.fail("RED: D-LOC-01 — 구현 없음, 의도적 실패")` |

---

## 4. RED 스켈레톤 산출물

### 4.1 테스트 (tests/만)

| 파일 | 역할 |
|------|------|
| `tests/conftest.py` | `grid_g1` fixture, G1 격자, `entity.constants.BLANK` |
| `tests/entity/test_d_loc_01.py` | D-LOC-01, AAA 주석, `pytest.fail` 한 줄 |

**규칙 준수:** assert 본문·skip·xfail·통과 더미 없음. `find_blank_coords` 호출 코드 없음(주석만).

### 4.2 상수 SSOT (픽스처 import용)

| 파일 | 역할 |
|------|------|
| `src/entity/constants.py` | `SIZE`, `CELL_MAX`, `MAGIC_SUM`, `BLANK`, `BLANK_COUNT` |

> RED 스켈레톤 명령은 `src/` 구현 금지였으나, **픽스처가 `entity.constants` import**를 요구하여 상수 파일만 추가. `locator`·`find_blank_coords` **없음**.

### 4.3 Harness 보완

| 파일 | 변경 |
|------|------|
| `pyproject.toml` | `addopts = ["--import-mode=importlib"]` — `tests/entity/` ↔ `src/entity/` 이름 충돌 방지 |

---

## 5. pytest 결과

```text
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
→ FAILED — Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패
```

| 항목 | 값 |
|------|-----|
| Test ID | D-LOC-01 |
| 수집 | 1 |
| 통과 | 0 |
| RED 판정 | ✅ 의도적 FAIL |

---

## 6. 세션 내 기타 작업 (요약)

| 작업 | 결과 |
|------|------|
| GitHub 공개 저장소 목록 (jh9009) | 5개 repo 확인 |
| `/red-test-plan` D-LOC-01 | C2C 표·Track B·플랜 (Ask, 파일 미생성) |
| Track A U-IN-01/02 | boundary RED 설계표 (Ask) |
| Logic vs Boundary RED 표 | Dual-Track 계획표 (Ask) |
| 전체 스켈레톤 + README | 작성 후 **사용자 요청으로 삭제**, README만 유지 |
| `/red-skeleton` D-LOC-01 | **본 보고서 범위** |

---

## 7. ECB · Mock 점검

| 체크 | 결과 |
|------|------|
| Logic Track · Domain Mock | ✅ 없음 |
| entity E001~E005 | ✅ 테스트·소스 없음 |
| import 방향 | ✅ `tests` → `entity.constants` only |
| boundary/control 선행 구현 | ✅ 없음 |

---

## 8. 8계층 · Test Loop

| 계층 | 상태 | 비고 |
|------|------|------|
| Rule | ✅ | `.cursorrules`, constants SSOT |
| Test Loop | ⚠️ | **D-LOC-01 RED FAIL 1건** 확보; D-VAL-01·U-IN 미작성 |
| Skill / Command | ✅ | `/tdd-red`, `magic-square-tdd` |
| Hook | ✅ | sessionStart, postToolUse |
| Entity 구현 | ❌ | `find_blank_coords` GREEN 대기 |

---

## 9. 다음 단계 (권장)

| 순서 | 작업 |
|------|------|
| 1 | **GREEN** `entity/locator.py` — `find_blank_coords` 최소 구현 |
| 2 | D-LOC-01: `pytest.fail` → assert `[(2,2),(3,3)]` |
| 3 | D-LOC-02/03 RED 스켈레톤 (선택) |
| 4 | control **D-VAL-01** RED (PRD T1, Mom Test 핵심) |
| 5 | boundary **U-IN-01/02** RED |

---

## 10. 참고 문서

- `README.md` — 프로젝트 구조·pytest 예시
- `docs/PRD.md` — FR-4 T1~T3
- `.cursor/skills/magic-square-tdd/reference.md` — D-* ID
- `Report/03.MagicSquare_Session3_Cursor_Design_Report.md`

---

*End of Report 04*
