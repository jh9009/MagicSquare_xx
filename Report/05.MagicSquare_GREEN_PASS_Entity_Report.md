# MagicSquare_xx — Entity GREEN PASS 게이트 보고서

| 항목 | 내용 |
|------|------|
| Phase | **GREEN** |
| Layer / Track | **entity** / **Logic** |
| 작성일 | 2026-06-04 |
| Golden Master | `tests/golden/d_sol_01_g1_step_a.approved.txt` |

---

## 1. GREEN PASS 체크리스트

| Test ID | 파일 | 상태 | 비고 |
|---------|------|------|------|
| **D-LOC-01** | `test_d_loc_01.py` | ✅ PASS | `find_blank_coords` → `[(2,2),(3,3)]` |
| **D-SOL-01** | `test_d_sol_01.py` | ✅ PASS | `solve_step_a` → `[2,2,10,3,3,7]` |
| **D-SOL-01 Golden** | 동일 | ✅ **matched** | `STATUS=OK` / `RESULT=2,2,10,3,3,7` |

**금지 준수:** `pytest.fail`·skip·xfail 없음 · entity E001~E005 없음 · boundary/control import 없음

---

## 2. 구현 (src/entity)

| 모듈 | 함수 | 역할 |
|------|------|------|
| `constants.py` | SSOT | `BLANK`, `SIZE`, `MAGIC_SUM`, … |
| `locator.py` | `find_blank_coords` | D-LOC-01 |
| `solver.py` | `solve_step_a` | D-SOL-01 → `int[6]` 1-index |

---

## 3. 검증 명령 (가상환경)

```powershell
Set-Location MagicSquare_xx
.\scripts\verify_green_entity.ps1
```

또는:

```powershell
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[dev]"
python -m pytest tests/entity/ -v
python -m pytest tests/entity/test_d_sol_01.py::test_d_sol_01_step_a_success -v -s
# stdout: golden matched: d_sol_01_g1_step_a.approved.txt
```

Golden 기준 갱신 (필요 시만):

```powershell
$env:UPDATE_GOLDEN = "1"
python -m pytest tests/entity/test_d_sol_01.py::test_d_sol_01_step_a_success -v
Remove-Item Env:UPDATE_GOLDEN
```

---

## 4. Golden Master

| 경로 | 내용 |
|------|------|
| `tests/golden/d_sol_01_g1_step_a.approved.txt` | `STATUS=OK` + `RESULT=2,2,10,3,3,7` |
| `tests/_approval.py` | `assert_matches_golden` |
| `tests/golden_format.py` | `format_solve_step_ok` |

---

## 5. 다음 단계 (Golden Master 이후)

| 순서 | 작업 |
|------|------|
| 1 | control **D-VAL-01** RED → GREEN |
| 2 | boundary **U-IN-01/02** RED → GREEN |
| 3 | REFACTOR (동작 동일, SSOT·의존 정리) |

---

*End of Report 05*
