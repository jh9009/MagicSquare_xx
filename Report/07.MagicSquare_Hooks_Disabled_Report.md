# MagicSquare_xx — Cursor Hooks 비활성화 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_xx |
| 문서 | `07.MagicSquare_Hooks_Disabled_Report.md` |
| 작성일 | 2026-06-04 |
| 사유 | Windows에서 hook 실행 시 **콘솔 창**이 반복적으로 뜨는 현상 |
| Transcript | `Prompting/03` Turn 49 |

---

## 1. 변경 요약

| 항목 | 이전 | 현재 |
|------|------|------|
| `.cursor/hooks.json` | `sessionStart` + `postToolUse` (Write) | **`"hooks": {}`** (비활성) |
| 백업 | — | `.cursor/hooks.enabled.json` (이전 설정 전체) |
| 스크립트 | `session-init.sh`, `hint-after-src-test-write.sh` | **삭제하지 않음** — 재활성 시 사용 |

---

## 2. 영향

| 유지됨 | 없어짐 (hook 전용) |
|--------|-------------------|
| `.cursorrules` | 세션 시작 `additional_context` 자동 주입 |
| Skill `magic-square-tdd` | `src/`·`tests/` Write 후 pytest 힌트 |
| Command `/tdd-red`, `/review-ecb` | `touched_src_tests.flag` 터치 |
| pytest · entity GREEN | — |

TDD·ECB·테스트 동작에는 **영향 없음**.

---

## 3. Hooks 다시 켜기

1. `.cursor/hooks.enabled.json` 내용을 `.cursor/hooks.json`에 복사  
2. 또는 `hooks.json`의 `hooks` 객체에 `sessionStart` / `postToolUse` 배열 복원  
3. Cursor **새 채팅** 또는 재시작 후 반영

---

## 4. 역사 보고서와의 관계

| 문서 | 내용 |
|------|------|
| `Report/03` | 세션 3 당시 Hook **구현·설계** 기록 (스냅샷) |
| `Prompting/01` Turn 31–33 | Hook **도입** 과정 |
| `Report/07` (본 문서) | **운영상 비활성** 기준 시점 |

---

*End of Report 07*
